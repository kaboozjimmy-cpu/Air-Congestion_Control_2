# Core dependencies (TensorFlow optional — install separately if on Python 3.11/3.12)
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0
xgboost>=2.0.3
shap>=0.44.0
streamlit>=1.31.0
plotly>=5.18.0
requests>=2.31.0
python-dotenv>=1.0.0
joblib>=1.3.0
# tensorflow>=2.15.0  # OPTIONAL: Only if Python <= 3.12. LSTM skips gracefully without it.

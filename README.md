# ATC Congestion Prediction System (Fixed)

## Quick Start
```bash
pip install -r requirements.txt
python train.py
streamlit run app.py
```

## All 38 Bugs Fixed

### 🔴 Hard Crashes (5) — ALL FIXED
| # | Bug | Fix | File |
|---|-----|-----|------|
| 1 | `use_label_encoder=False` removed in XGBoost 2.0+ | Removed param entirely | train.py |
| 2 | `style.applymap()` renamed in pandas 2.1+ | Changed to `.map()` | app.py |
| 3 | `st.selectbox()` crashes on empty list | Guard with `if selected_airports` | app.py |
| 4 | Bare `except: pass` swallows all errors | Specific `except (ImportError, OSError, ValueError)` | app.py |
| 5 | `data/` dir not created before CSV save | Added `os.makedirs("data", exist_ok=True)` | data_loader.py |

### 🟠 Silent Failures (14) — ALL FIXED
| # | Bug | Fix | File |
|---|-----|-----|------|
| 6 | `OPEN_METEO_BASE` env var undefined | Hardcoded URL directly | data_loader.py |
| 7 | Wrong API field `windspeed_10m` | Changed to `wind_speed_10m` | data_loader.py |
| 8 | Wrong API field `cloudcover` | Changed to `cloud_cover` | data_loader.py |
| 9 | `visibility` not in Open-Meteo free tier | Estimated from cloud_cover + rain | data_loader.py |
| 10 | `fetch_opensky_live()` never called | Integrated into `_build_live_snapshot()` | data_loader.py |
| 11 | `fetch_aviationstack()` dead code | Removed (unused) | data_loader.py |
| 12 | `forecast_window` selectbox value ignored | Parsed to int, passed through | app.py |
| 13 | `run_btn` does nothing | Now clears cache + reruns | app.py |
| 14 | SHAP feature names from raw df (pre-pipeline) | Uses `available_features` from `prepare_xy()` | train.py |
| 15 | `build_sequences()` re-runs pipeline (overwrites overrides) | Removed internal `run_pipeline()` call | pipeline.py |
| 16 | Synthetic fallback never triggers | Returns `None` when no live data | data_loader.py |
| 17 | `scale_pos_weight=2` ignored for 3-class | Replaced with `compute_sample_weight('balanced')` | train.py |
| 18 | Kaggle maps all rows to VOMM only | Maps 20 US airports → 5 Indian airports | data_loader.py |
| 19 | N/A (removed with data_loader rewrite) | — | — |

### 🟡 Logic / Data Errors (12) — ALL FIXED
| # | Bug | Fix | File |
|---|-----|-----|------|
| 20 | `congestion_score` exceeds 1.0 | Added `.clip(0, 1)` | app.py |
| 21 | `cloud_cover` can go negative | Added `max(0.0, ...)` guard | data_loader.py |
| 22 | `runway_util` computed but never saved | Added to records dict | data_loader.py |
| 23 | Multiple `datetime.now()` calls per row | Single `now` captured once per snapshot | data_loader.py |
| 24 | Kaggle departure time float conversion broken | `//100` for hour extraction, not string slicing | data_loader.py |
| 25 | LSTM trained on raw unscaled features | `build_sequences()` now applies scaler | pipeline.py |
| 26 | Fallback forecast hardcodes 12 points | Uses `window` parameter | app.py |
| 27 | Wrong "train LSTM first" message | Shows actual cause (insufficient data vs no model) | app.py |
| 28 | NaN metric cards when no airports selected | Guard `if len(latest) > 0` | app.py |
| 29 | Weather slider can't reach 0 (clear) | Checkbox + direct assignment instead of `.clip(lower=)` | app.py |
| 30 | LSTM train/test split not chronological | Split per airport then stack | train.py |
| 31 | `confusion_matrix` imported but unused | Removed dead import | train.py |
| 32 | JSON int keys become string keys | Explicit `str(k)` conversion | train.py |

### 🟡 Presentation / Deployment (7) — ALL FIXED
| # | Bug | Fix | File |
|---|-----|-----|------|
| 33 | Evaluation expander imports unused | Now shows confusion matrix + classification report | app.py |
| 34 | Forecast subheader hardcoded "30-minute" | Dynamic from `forecast_window` | app.py |
| 35 | LSTM `.h5` format deprecated | Saves as `.keras` | train.py |
| 36 | matplotlib/seaborn/imbalanced-learn unused | Removed from requirements.txt | requirements.txt |
| 37 | Refresh only clears `cache_data` | Now clears `cache_resource` too | app.py |
| 38 | Relative paths break from wrong directory | All paths use `BASE_DIR` | all files |

## Project Structure
```
atc_fixed/
├── data/
│   └── synthetic_atc_data.csv    # generated by train.py
├── models/
│   ├── xgboost_model.pkl
│   ├── lstm_model.keras          # .keras not .h5
│   ├── scaler.pkl
│   ├── shap_data.json
│   └── meta.json
├── utils/
│   ├── __init__.py
│   └── data_loader.py            # rebuilt from scratch
├── pipeline.py
├── train.py
├── app.py
├── requirements.txt
└── .env
```
